import { FileExtValidatorDirective } from './ext.directive';

describe('FileExtValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileExtValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
